{
  "name": "elm-parse-link-header",
  "private": true,
  "scripts": {
    "test": "npx elm-test",
    "format": "npx elm-format src/ tests/ --elm-version=0.19 --validate"
  },
  "devDependencies": {
    "elm": "^0.19.0",
    "elm-format": "^0.8.0",
    "elm-test": "^0.19.0-beta4"
  }
}
